import {colors} from './colors';

// Shared visual surfaces; no state or business rules.
export const cardSurface = {
  backgroundColor: colors.surface,
  borderColor: colors.border,
  borderWidth: 1,
  borderRadius: 16,
  padding: 16,
  marginBottom: 12,
};
